#include "stdio.h"

int main() {
	int a, b, pow = 1;

	printf("Digite o primeiro número: ");
	scanf("%d", &a);
	printf("Digite o segundo número: ");
	scanf("%d", &p);

	for (int i = 1; i <= b; i++) {
		pow = pow * a;
	}

	printf("%d", a);
}
